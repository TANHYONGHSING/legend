if(re.search(isAllPresent1 , name)):
        print("You have logged into the system.")
    else:
        print("No")

        def isAllPresent1(name):
 
    regex1 = ("([A-Z]) ([a-z])")

    p1 = re.compile(regex1)